import HeroImage from "./HeroImage";
import HeroDescription from "./HeroDescription";

const Hero =  () => {
    return (
        <section className="hero">
            <HeroImage />
            <HeroDescription />
        </section>
    );
};

export default Hero;