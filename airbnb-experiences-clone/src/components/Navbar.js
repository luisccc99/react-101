import {ReactComponent as Logo} from '../images/airbnb.svg';

const Navbar = () => {
    return (
        <nav className="mainNav">
            <Logo />
        </nav>
    );
};

export default Navbar;