import group from '../images/group.png';

const HeroImage = () => {
    return (
        <div>
            <img src={group} className="hero__image responsive"/>
        </div>
    );
};

export default HeroImage;